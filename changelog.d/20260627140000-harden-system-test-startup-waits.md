- Harden system-test startup against silent hangs: reject fast when the client WebSocket server cannot bind its port (e.g. `EADDRINUSE` from a leftover process) instead of waiting forever on the `listening` event, request Selenium's `eager` page load strategy so navigation does not block on the full `load` event held open by the app's persistent WebSocket/Scoundrel connections, and bound the Selenium page-load timeout so a stalled navigation fails fast with a specific error instead of an opaque suite timeout.
