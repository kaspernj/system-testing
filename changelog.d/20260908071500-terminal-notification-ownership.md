Preserve primary system-test failures alongside screenshot and teardown failures. Notification deadlines now guard actual Selenium command issue, including retained elements, and emit bounded payload-free issue/settlement evidence. Only pending commands make a notification deadline terminal. Terminal sessions expose the runner-independent `terminalResource` run contract and do not automatically restart; driver teardown is attempted once even when it fails. Consumers must use a runner that stops retries/later callbacks and reports not-run cases for this contract.
