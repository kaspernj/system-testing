- Size the shared system-test startup `beforeAll` timeout from the sum of `SystemTest.start()`'s bounded phases (Selenium session creation, initial navigation, `systemTestingComponent` lookup, and the client WebSocket connect) plus headroom. Previously the 120s startup budget was smaller than that sum, so under slow CI the outer hook hit jasmine's opaque suite timeout first and cascaded "beforeAll failed" across the whole run instead of surfacing the specific failing phase.
