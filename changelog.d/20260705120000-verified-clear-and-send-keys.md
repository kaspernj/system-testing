Add an opt-in `replaceInputValue(elementOrIdentifier, nextValue)` that replaces an input's value with chord-free, verified key presses: clear per character on both sides of the caret with BACK_SPACE and DELETE presses (select-all chords silently no-op on some headless CI Chrome sessions while subsequent typing still lands, and the focusing click can land the caret mid-value in multiline textareas), verify the field is empty before typing, and throw with the expected and actual values when the replacement never reaches the field. `clearAndSendKeys`/`replaceTestIDInputValue` keep their standard select-all + sendKeys semantics.

Add an opt-in `checkInterception: true` click/interact argument that hit-tests `method: "actions"` / `method: "human"` clicks before performing them and fails with an `ElementClickInterceptedError`-style error when another element would receive the click, matching `element.click()` interception semantics instead of silently dropping the press on an overlay. Native Appium app sessions skip the DOM hit-test.

Add `clickAndWaitForEffect(elementOrIdentifier, expectedEffectCallback, args)` so callers can await a caller-observable effect of a click and re-click while the effect has not appeared, closing the silent-drop failure mode where a click reports success but the press effect never happens. Each effect probe is clamped to the remaining overall `timeout`.
