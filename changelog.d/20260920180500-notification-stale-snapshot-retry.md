Tolerate stale element references in the one-shot notification snapshot paths: `notificationMessages()` and `dismissNotificationMessages()` now re-take their snapshot on a fresh pass (bounded, with a short settle) when a flash notification auto-dismisses mid-snapshot, instead of failing the cleanup path. `notificationMessages()` reports a persistent race instead of fabricating an empty result; `dismissNotificationMessages()` settles to its disappearance wait.
